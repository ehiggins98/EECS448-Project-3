* Code reader
	* Read code using Google Cloud Vision or ML Kit
	* Fix errors if possible
		* Correct variable names that are close to each other
		* Fix weird bracketing (like } being read as ) )
* Code execution
	* Return results of code execution
	* Run code
	* Run main/first function if there is one
* Mobile app
	* Can take a picture of code
	* Allow the user to correct code after it's parsed by Cloud Vision and error checked
	
